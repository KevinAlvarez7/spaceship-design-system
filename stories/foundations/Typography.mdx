import { Meta, Typeset } from '@storybook/addon-docs/blocks';
import { fontSizes, fontWeights, lineHeights, fontFamilies } from '@/tokens';

<Meta title="Foundations/Typography" />

export const pxSizes = [11, 12, 14, 16, 18, 20, 24, 30, 36, 48, 60, 72, 96, 128];

# Typography

## Typefaces

**Primary (Serif):** Zilla Slab — display text, editorial headings

<Typeset
  fontFamily="Zilla Slab, Georgia, serif"
  fontSizes={pxSizes}
  fontWeight={700}
  sampleText="The quick brown fox jumps over the lazy dog"
/>

**Secondary (Sans):** Outfit — UI text, body, labels

<Typeset
  fontFamily="Outfit, system-ui, sans-serif"
  fontSizes={pxSizes}
  fontWeight={400}
  sampleText="The quick brown fox jumps over the lazy dog"
/>

**Mono:** JetBrains Mono — code, tokens, technical labels

<Typeset
  fontFamily="JetBrains Mono, ui-monospace, monospace"
  fontSizes={[11, 12, 13, 14, 16]}
  fontWeight={400}
  sampleText="const token = 'var(--font-size-base)'; // 1rem"
/>

## Font Sizes

| Name | CSS Var | Value |
|------|---------|-------|
{fontSizes.map(t => `| \`${t.name}\` | \`${t.cssVar}\` | ${t.value} |`).join('\n')}

## Font Weights

| Name | CSS Var | Value |
|------|---------|-------|
{fontWeights.map(t => `| \`${t.name}\` | \`${t.cssVar}\` | ${t.value} |`).join('\n')}

## Line Heights

| Name | CSS Var | Value |
|------|---------|-------|
{lineHeights.map(t => `| \`${t.name}\` | \`${t.cssVar}\` | ${t.value} |`).join('\n')}
