import { Meta } from '@storybook/addon-docs/blocks';
import { shadowTokens } from '@/tokens';

<Meta title="Foundations/Shadow" />

# Shadow

Elevation tokens using the Jakub 3-layer shadow system.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: '24px', marginTop: '16px' }}>
  {shadowTokens.map(token => (
    <div key={token.cssVar} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '16px', padding: '24px', borderRadius: '8px', background: '#f9fafb', border: '1px solid #f3f4f6' }}>
      <div style={{ height: '64px', width: '64px', borderRadius: '12px', background: 'white', boxShadow: `var(${token.cssVar})` }} />
      <div style={{ textAlign: 'center' }}>
        <div style={{ fontSize: '13px', fontWeight: 600, color: '#111827', fontFamily: 'monospace' }}>{token.name}</div>
        <div style={{ fontSize: '11px', color: '#9ca3af', fontFamily: 'monospace', marginTop: '2px' }}>{token.cssVar}</div>
        {token.description && <div style={{ fontSize: '12px', color: '#6b7280', marginTop: '4px' }}>{token.description}</div>}
      </div>
    </div>
  ))}
</div>
