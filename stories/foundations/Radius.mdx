import { Meta } from '@storybook/addon-docs/blocks';
import { radiusTokens } from '@/tokens';

<Meta title="Foundations/Radius" />

# Radius

Tailwind v4 built-in border-radius scale. Use native `rounded-*` utilities directly. No custom CSS variables.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(130px, 1fr))', gap: '16px', marginTop: '16px' }}>
  {radiusTokens.map(token => (
    <div key={token.name} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '12px', padding: '16px', borderRadius: '8px', border: '1px solid #e5e7eb', background: 'white' }}>
      <div style={{ height: '56px', width: '56px', background: '#dbeafe', border: '2px solid #93c5fd', borderRadius: token.value === '9999px' ? '9999px' : token.value }} />
      <div style={{ textAlign: 'center' }}>
        <div style={{ fontSize: '13px', fontWeight: 600, color: '#111827', fontFamily: 'monospace' }}>{token.name}</div>
        <div style={{ fontSize: '11px', color: '#9ca3af', fontFamily: 'monospace', marginTop: '2px' }}>{token.value}</div>
        <div style={{ fontSize: '11px', color: '#6b7280', fontFamily: 'monospace', marginTop: '2px' }}>{token.twClass}</div>
      </div>
    </div>
  ))}
</div>
